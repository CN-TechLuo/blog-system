name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.CI_DB_PASSWORD || 'ci-db-password' }}
          MYSQL_DATABASE: blog_db
        ports:
          - 3306:3306
        options: --health-cmd "mysqladmin ping -h localhost" --health-interval 10s --health-timeout 5s --health-retries 10

    steps:
      - uses: actions/checkout@v5
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Initialize database schema
        run: |
          for i in $(seq 1 30); do
            if mysql -h 127.0.0.1 -u root -p${{ secrets.CI_DB_PASSWORD || 'ci-db-password' }} -e "SELECT 1" > /dev/null 2>&1; then
              echo "MySQL is ready"
              break
            fi
            echo "Waiting for MySQL... ($i/30)"
            sleep 2
          done
          mysql -h 127.0.0.1 -u root -p${{ secrets.CI_DB_PASSWORD || 'ci-db-password' }} < src/main/resources/db/schema.sql
      - name: Build and test with Maven
        run: ./mvnw verify
        env:
          DB_USERNAME: root
          DB_PASSWORD: ${{ secrets.CI_DB_PASSWORD || 'ci-db-password' }}
          JWT_SECRET: ${{ secrets.CI_JWT_SECRET || 'Y2ktand0LXNlY3JldC1mb3ItY2ktb25seS0zMi1ieXRlcyE=' }}
          SPRING_DATASOURCE_URL: jdbc:mysql://127.0.0.1:3306/blog_db?useSSL=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8

  docker:
    needs: build
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to GHCR (tags only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          tags: blog-system:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Push image to GHCR (tags only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
            ghcr.io/${{ github.repository }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
